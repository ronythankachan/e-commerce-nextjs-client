import React, { useEffect, useState } from "react";

const ProtectLayer = () => {
  const [user, setUser] = useState({});
  useEffect(() => {}, []);

  return <div></div>;
};

export default ProtectLayer;
