import React from "react";
import Layout from "../components/client/Layout";

const orders = () => {
  return <Layout>
      <main className="mt-16 bg-gray-50 min-h-screen"> Orders page</main>
  </Layout>;
};

export default orders;
